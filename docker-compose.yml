services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: biaoqiao
      POSTGRES_USER: biaoqiao
      POSTGRES_PASSWORD: biaoqiao_dev
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./sql:/docker-entrypoint-initdb.d
    command: >
      postgres
        -c shared_buffers=512MB
        -c effective_cache_size=4GB
        -c work_mem=64MB
        -c max_connections=200
    ports: ["5432:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U biaoqiao"]
      interval: 5s
      retries: 5

  elasticsearch:
    image: biaoqiao-elasticsearch:8.15.0-ik
    build:
      context: ./docker/elasticsearch
      args:
        ELASTICSEARCH_BASE_IMAGE: ${ELASTICSEARCH_BASE_IMAGE:-docker.elastic.co/elasticsearch/elasticsearch:8.15.0}
        IK_PLUGIN_URL: ${IK_PLUGIN_URL:-https://get.infini.cloud/elasticsearch/analysis-ik/8.15.0}
    restart: always
    environment:
      - discovery.type=single-node
      - ES_JAVA_OPTS=-Xms2g -Xmx2g
      - xpack.security.enabled=false
    ports: ["9200:9200"]
    volumes:
      - es_data:/usr/share/elasticsearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1"]
      interval: 10s
      retries: 10

  backend:
    image: biaoqiao-backend:dev
    build:
      context: ./backend
      args:
        PYTHON_BASE_IMAGE: ${PYTHON_BASE_IMAGE:-python:3.12-slim}
        PIP_INDEX_URL: ${PIP_INDEX_URL:-}
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4 --reload
    volumes:
      - ./backend:/app
      - ./previews:/data/previews
      - ./特效/merged/gifs:/data/gifs:ro
    environment:
      - DATABASE_URL=postgresql://biaoqiao:biaoqiao_dev@postgres:5432/biaoqiao
      - ES_URL=http://elasticsearch:9200
      - ADMIN_API_KEY=dev-admin-key-change-in-prod
    depends_on:
      postgres:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy

  nginx:
    image: nginx:alpine
    ports: ["80:80"]
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./frontend/dist:/usr/share/nginx/html:ro
      - ./previews:/data/previews:ro
    depends_on: [backend]

volumes:
  pg_data:
  es_data:
